// a => i32
let a = 10

// big => i64 (exceeds 2^31-1)
let big = 10000000000

// f => f32 (decimal literal)
let f = 3.14

// sum => a + f => i32 + f32 => f32
let sum = a + f
print sum

// x => i32
let x = 42

// y => f32 (decimal literal)
let y = 2.5

// product => x * y => i32 * f32 => f32
let product = x * y
print product
