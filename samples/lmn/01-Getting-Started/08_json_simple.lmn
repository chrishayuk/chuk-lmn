# 08_json_simple.lmn
function main()
    # A simple JSON object assigned to a variable
    let user = {
        "name": "Alice",
        "age": 42,
        "active": true
    }

    # Now we can use 'user' like any JSON value
    print "User object is" user
end